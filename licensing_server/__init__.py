"""Server-side product licensing tooling.

This package is intentionally separate from ``src/autotube`` and is never
imported by the distributed AutoTube client. It owns the private signing key
and product-key issuance/revocation logic.
"""
